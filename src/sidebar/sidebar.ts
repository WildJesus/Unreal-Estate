// Sidebar UI — injected into the sreality.cz page as a floating panel
// Responsibilities:
//   - Render universe switcher controls
//   - Render salary input options
//   - Emit events when the user changes universe or salary settings
//
// No UI logic yet — skeleton only.

console.log("[Srealitky Universes] sidebar loaded");

// TODO: render sidebar DOM
// TODO: universe switcher (historical year slider, cross-city selector)
// TODO: salary input (median default → percentile → field → exact)
// TODO: emit CustomEvent on change so content.ts can react
